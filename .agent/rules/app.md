---
trigger: always_on
---

- Always **check the Design Tokens** (e.g. `global.css`, theme files, or token definitions) **before starting any frontend implementation**.
- Ensure that all UI components strictly follow the existing **design system and tokens** (colors, spacing, typography, etc.).
- Do not introduce hard-coded styles if a corresponding design token already exists.
